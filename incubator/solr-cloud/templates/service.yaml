apiVersion: v1
kind: Service
metadata:
  name: {{ template "solr-cloud.fullname" . }}
  labels:
    app: {{ template "solr-cloud.name" . }}
    chart: {{ template "solr-cloud.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "solr-cloud.name" . }}
    release: {{ .Release.Name }}
